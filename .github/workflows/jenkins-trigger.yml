name: Trigger Jenkins Pipeline

on:
  push:
    branches:
      - main

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST "http://34.22.109.154:8080/job/be4man-target-server/build?token=deploy" \
          --user "admin:${{ secrets.JENKINS_TOKEN }}"
